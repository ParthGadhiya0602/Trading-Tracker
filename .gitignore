# Secrets & local configuration (keep .env.sample tracked)
.env
.env.*
!.env.sample

# Runtime / generated data
store/*
license.key
logs/

# Data-source reference (kept local only, not published)
NSE_API.md
AUTH_DESIGN.md
ALERT_LIFECYCLE_PLAN.md
REORG_PLAN.md

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp

# Logs
*.log

.impeccable

# Node (not used today, but safe if deps are ever added)
node_modules/
npm-debug.log*
