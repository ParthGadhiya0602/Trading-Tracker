{
  "name": "trading-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "NIFTY OHL dashboard + server-side price alerts (local data proxy)",
  "type": "commonjs",
  "scripts": {
    "start": "node backend/server.js",
    "live": "STREAM_WS=1 node backend/server.js",
    "closed": "node backend/server.js",
    "test": "node --test backend/*.test.js"
  },
  "dependencies": {
    "mongodb": "^6.9.0"
  }
}
